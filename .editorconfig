# top-most EditorConfig file
root = true

# Unix-style newlines with a newline ending every file
[*]
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true

# use tabs by default for code
[*.{go}]
indent_style = tab
indent_size = 2